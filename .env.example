# ===========================================
# Grand Canyon - Environment Configuration
# ===========================================

# Supabase Configuration (Required)
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role (Optional)
# Only needed for admin operations like user creation
# Keep this secret - never expose in client code!
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Email Configuration (Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# ===========================================
# Environment Variable Reference
# ===========================================
#
# | Variable                        | Required | Description                              |
# |---------------------------------|----------|------------------------------------------|
# | NEXT_PUBLIC_SUPABASE_URL        | Yes      | Supabase project URL                     |
# | NEXT_PUBLIC_SUPABASE_ANON_KEY   | Yes      | Anonymous key (safe for browser)         |
# | SUPABASE_SERVICE_ROLE_KEY       | No       | Admin key for server-side operations     |
#
# ===========================================
# Production Notes
# ===========================================
# 1. NEXT_PUBLIC_ vars are exposed to the browser - only use for public keys
# 2. Never commit .env.local to git (already in .gitignore)
# 3. Set production vars in Vercel dashboard: Project > Settings > Environment Variables
# 4. Each environment (Preview, Production) can have different values
